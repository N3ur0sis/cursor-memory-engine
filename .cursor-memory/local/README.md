# Local Memory

Keep this directory private to each developer.

- `session.jsonl`: active task packets and short-term conversation carry-over
- `user-preferences.jsonl`: personal workflow preferences, answer style, and local defaults

The `.gitignore` in this directory keeps these files out of git after the starter pack is copied into another repository.
